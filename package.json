{
  "name": "@app-shack/react-native-packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "yarn workspaces foreach -A run lint",
    "tsc": "yarn workspaces foreach -A run tsc",
    "react-native-eslint": "yarn workspace @app-shack/react-native-eslint"
  },
  "devDependencies": {
    "eslint": "9.27.0",
    "husky": "9.1.7"
  },
  "packageManager": "yarn@4.9.1"
}
